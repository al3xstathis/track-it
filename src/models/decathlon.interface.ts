export interface IDecathlon {
    hundred: string;
    lj: string;
    sp: string;
    hj: string;
    four: string;
    hurdles: string;
    dt: string;
    pv: string;
    jt: string;
    fifteen: string;
    dayOneScore: string;
    dayTwoScore: string;
    totalScore: string;


    // calculateRunning(score,a,b,c);
    // calculateField(score,a,b,c);
    // eventScore(event,score);
    // getSeconds: (number) =>number;
}




